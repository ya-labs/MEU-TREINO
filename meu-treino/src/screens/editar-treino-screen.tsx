import { ScreenContainer } from '@/components/common';
import type { RootStackScreenProps } from '@/navigation/navigation-types';

export default function EditarTreinoScreen(_props: RootStackScreenProps<'EditarTreino'>) {
  return <ScreenContainer title="Editar Treino" />;
}
