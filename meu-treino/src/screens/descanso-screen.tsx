import { ScreenContainer } from '@/components/common';
import type { RootStackScreenProps } from '@/navigation/navigation-types';

export default function DescansoScreen(_props: RootStackScreenProps<'Descanso'>) {
  return <ScreenContainer title="Descanso" />;
}
